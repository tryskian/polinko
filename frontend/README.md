# Frontend (Vite)

## Dev

From repo root:

1. `make server`
2. `make ui-install`
3. `npm run dev`
4. open `http://127.0.0.1:5173`

## Backend Config

Set in `.env`:

- `OPENAI_API_KEY`

Optional:

- `POLINKO_SERVER_API_KEY` (if set, enter same value in frontend x-api-key field)
