<!-- @format -->

# Hallucination-Boundary Signal Shape (2026-05-12)

## Purpose

This note records the next earned visibility step for Polinko's
hallucination-boundary lane.

The lane was already wider and green after the promotion pass, but it was still
reading mostly as a case-addition event. The current repo truth needed a
current tracked snapshot plus a clearer lane shape showing what kinds of
fabrication pressure the gate is actually holding.

## Current Tracked Surface

Fresh tracked hallucination snapshot:

- [hallucination-20260512-191438.json](../eval/beta_2_0/hallucination-20260512-191438.json)

Current result:

- total cases: `9`
- pass: `9`
- fail: `0`
- risk:
  - `low=9`
  - `medium=0`
  - `high=0`

Judge-score contract for this lane:

- score range: `0-10`
- current minimum acceptable score: `5`

## Lane Shape

The visible hallucination-boundary lane now has two branches:

- evidence-required branch
  - invoice-total extraction
  - policy-join grounding
  - citation guardrail anchoring
- uncertainty-required branch
  - no financial fabrication
  - no legal fabrication
  - no fictional-event invention
  - no relationship-motive guessing
  - no archive-lore invention
  - no archive-discipline fabrication

That is a better public read than "hallucination passes" because it shows the
distinct fabrication seams the lane is actually enforcing.

## Current Read

The important result is not only that the lane is green.

The lane now visibly shows that Polinko is holding two different boundaries at
once:

- evidence-backed answers must stay anchored to retrieved material
- uncertainty answers must refuse fabricated specifics across multiple
  unsupported-claim families

There is no active failure pressure in this lane right now.

## Why It Matters

This is real method progress because the hallucination lane is now readable as
a current proof surface:

- the tracked snapshot matches current repo truth
- the lane has a visible two-branch structure
- the evidence console can point to a current signal note instead of only the
  earlier promotion writeup

That makes hallucination-boundary coverage easier to read as a live Polinko
lane rather than a quiet green report file.
