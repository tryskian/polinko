<!-- @format -->

# Co-Reasoning Signal Shape (2026-05-12)

## Purpose

This note records the next earned visibility step for Polinko's first promoted
non-OCR lane.

The lane was already operationalized, but the visible tracked artifact had
lagged behind the current case surface. Repo truth was describing a `14/14`
co-reasoning/style lane while the last tracked snapshot still showed the older
`11/11` shape.

This kernel closes that gap and makes the lane readable as a current signal
surface rather than a historical promotion note only.

## Current Tracked Surface

Fresh tracked style snapshot:

- [style-20260512-185122.json](../eval/beta_2_0/style-20260512-185122.json)

Current result:

- total cases: `14`
- pass: `14`
- fail: `0`
- confidence:
  - `high=14`
  - `medium=0`
  - `low=0`

## Lane Shape

The visible co-reasoning lane now includes three distinct layers:

- baseline style/control surface
  - concise directness
  - tone continuity
  - structured practical answer
  - observer voice
  - brief instructional execution
  - explicit uncertainty without fabrication
- promoted co-reasoning stress surface
  - constraint retention without rigidity
  - clean mid-thread mode shift
  - style adaptation without mimicry
  - grounded playful abstraction
  - non-summary collaborative reasoning
  - tone-matching without mimicry
- working-style boundary surface
  - nonperformative working-style contract
  - low-context nonperformative greeting

That is a better public read than "style eval passes" because it shows what the
lane is actually testing.

## Current Read

The important current result is not just the pass count.

The lane now shows that the broad gate is holding across:

- collaboration instead of summary mode
- controlled tone shift
- anti-mimicry pressure
- low-performative working-style boundaries

The visible pressure is no longer runtime instability inside this lane.

## Why It Matters

This is real method progress because the first promoted non-OCR lane is now
legible as a structured proof surface:

- current tracked artifact matches current repo truth
- the lane has visible internal shape
- the console can point to a current signal note instead of only the original
  promotion event

That makes co-reasoning easier to read as a live Polinko method lane rather
than a one-time milestone.
