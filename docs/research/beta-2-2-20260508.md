<!-- @format -->

# Beta 2.2 Snapshot

Date: `2026-05-08`

## Boundary

`Beta 2.2` is Polinko's current serious method beta.

This is not a finished-product claim. It is the point where the repo has:

- an explicit `pass` / `fail` / `evict` gate contract
- first-gate contract correctness before richer interpretation
- one mature method lane that remains green
- one promoted non-OCR lane that is now operationalized in tracked evals

## What Makes This A Beta Boundary

- co-reasoning reliability is no longer only a hypothesis or backlog target
- the tracked style stress surface now carries the first promoted non-OCR lane
- the repo truth now encodes `evict` as upstream case correction rather than a
  fuzzy third gate state
- thin future lanes now have a clear promotion pattern:
  - human-owned row-local evidence first
  - broader automation later if the lane earns it

## Current Read

- gate contract:
  - `pass`
  - `fail`
  - `evict` upstream only
- co-reasoning style lane:
  - live pass: `14/14`
- current mature method lane:
  - OCR growth stability: `25/25`
  - fail-history cohort: `0` active cases
- next thinner lane:
  - operator burden

## Why It Matters

Polinko now reads as a serious beta of the method instead of a repo with one
dominant eval lane and a set of broader intentions around it.
